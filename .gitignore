# ---- CMake build output ----
# The entire build tree is generated by CMake and must never be committed.
build/
out/

build/
lib/bin/
*.dll

# ---- Compiled object files ----
*.o
*.obj

# ---- Executables ----
*.exe
*.out

# ---- MSVC debug / linker artifacts ----
*.pdb
*.ilk
*.exp

# ---- Editor / IDE (local settings) ----
.vscode/

# ---- OS junk ----
Thumbs.db
.DS_Store

# ---- Third-party OpenCV distribution ----
# Not committed to keep the repo lean. After cloning, place the OpenCV 4.5.1
# (MSVC build) headers in lib/include and the libraries in lib/bin.
lib/
